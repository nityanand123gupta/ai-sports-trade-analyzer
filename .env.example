# Required only for `--agentic` mode (CrewAI multi-agent narrative synthesis).
# The deterministic pipeline (default mode) needs no API key at all.
ANTHROPIC_API_KEY=

# Optional: override the default model used by the CrewAI agents.
TRADE_ANALYZER_MODEL=anthropic/claude-sonnet-4-5-20250929
